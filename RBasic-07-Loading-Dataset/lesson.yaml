- Class: meta
  Course: DataScienceAndR
  Lesson: RBasic-07-Loading-Dataset
  Author: Wush Wu
  Type: Standard
  Organization: Taiwan R User Group
  Version: 1.0
- Class: text
  Output: 在做資料分析之前，我們要能載入資料
- Class: text
  Output: 這個課程的目的是介紹R 最常見的幾種載入資料的方式
- Class: cmd_question
  Output: R 內建有許多知名的資料集。這些內建的資料集，目前都在`datasets`這個套件之中。 所以同學可以用輸入：`library(help=datasets)`來檢視所有的資料集。
    請同學試試看。
  CorrectAnswer: library(help=datasets)
  AnswerTests: omnitest('library(help=datasets)')
  Hint: library(help=datasets)
- Class: cmd_question
  Output: 在載入資料之前，為了等一下更清楚地說明R 的函數， 請同學先輸入：`iris <- 1`。
  CorrectAnswer: iris <- 1
  AnswerTests: any_of_exprs('iris <- 1', 'iris = 1')
  Hint: iris <- 1
- Class: cmd_question
  Output: 這些列出的資料中，我們可以運用`data`這個指令來載入。 請同學試試看：`data(iris, package = "datasets")`
    這個指令的意思就是，從套件datasets中載入資料集iris。
  CorrectAnswer: data(iris, package = "datasets")
  AnswerTests: omnitest('data(iris, package = "datasets")')
  Hint: data(iris, package = "datasets")
- Class: cmd_question
  Output: 接著，我們請同學輸入：`head(iris)`
  CorrectAnswer: head(iris)
  AnswerTests: omnitest('head(iris)')
  Hint: head(iris)
- Class: text
  Output: 同學應該會在螢幕上看到R 輸出一個data.frame，而不是 當初輸入的1
- Class: text
  Output: 這就是`data`函數的功能。它可以讓同學從套件載入特定資料 集（Data Sets）。舉例來說，我們剛才已由library(help=datasets)
    觀察到datasets套件中有包含許多資料集，例如:AirPassengers、BJsales、…、iris…等。 我們可用`data(iris, package
    = "datasets")`指令，其中第一個參數可指定特定資料集， R 則會把iris的資料集輸入到iris這個變數之中。
- Class: cmd_question
  Output: 接著，請同學查詢iris的欄位名稱。
  CorrectAnswer: colnames(iris)
  AnswerTests: omnitest(correctVal = colnames(iris))
  Hint: colnames(iris)
- Class: text
  Output: 雖然我們看到了欄位名稱，可是並不清楚這些欄位的意義， 也不清楚每列資料所代表的意義。這些事情，必須在實際進行資料 分析之前就要理解的。
- Class: cmd_question
  Output: 如果是使用R 提供的資料集，通常都會有相關的說明。 甚至會包含該資料集出處的論文。請同學輸入：`help(iris)`
  CorrectAnswer: help(iris)
  AnswerTests: any_of_exprs('help(iris)', '?iris')
  Hint: help(iris)
- Class: text
  Output: 以上練習，已經讓同學了解如何使用R 內建的資料集。
- Class: text
  Output: 接著，我們來展示如何讓同學讀取外部的資料集。
- Class: text
  Output: 外部資料集的格式有千百種，今天只跟大家介紹最常見的csv 檔。
- Class: text
  Output: csv 檔的格式是以行(Line)為單位。每一行(Line)就是一筆紀錄(Record)。而每一筆紀錄中， 是以一個分隔符號來讓使用者或程式分辨欄位。常見的分隔符號有：
    tab (\t)或逗號(,) 。 若以逗號做為分隔符號，則學生姓名加上4次小考分數的一筆紀錄，可用CSV表達如下： `柯小哲,77,82,87,92`
- Class: text
  Output: 我們拿一個自<http://data.gov.tw/node/6213>下載的csv 檔 做範例。這個檔案的目錄已經存入了`lvr_land.path`
    變數之中。 我們將利用這個檔案來示範如何載入中文資料到R 。
- Class: text
  Output: 載入中文資料，最重要的就是要抓對檔案所採用的中文的編碼。 可惜目前我還不清楚如何在R 裏面偵測編碼。另外同學如果不懂什麼是 編碼，可以參考這個youtube
    影片：<https://www.youtube.com/watch?v=GG2UFrmdtH4>
- Class: cmd_question
  Output: 簡單起見，我們這裡帶著同學測試這個檔案的編碼。 第一招，是檢查檔案有沒有BOM 。BOM 是一種利用檔案最前面的2 個或 3 個位元組來標示檔案編碼的方式。在windows
    底下建立的中文檔案， 有很高的機會會帶BOM 。請同學執行：`readBin(lvr_land.path, what = "raw", n = 3)`
  CorrectAnswer: readBin(lvr_land.path, what = "raw", n = 3)
  AnswerTests: omnitest(correctVal = charToRaw("\xb6m\xc2"))
  Hint: readBin(lvr_land.path, what = "raw", n = 3)
- Class: text
  Output: R 中的`readBin` 是一個很可以直接讀取檔案包含的位元的指令 ，所以可以正確的處理BOM 。第一個參數是檔案的路徑。第二個名稱為 what的參數，是告訴R
    要如何處理讀入的資料。`what = "raw"`代表讀 入的資料用位元處理，而不是文字。第三個名稱為n 的參數，代表要輸 入的長度。這裡選`n = 3`
    是因為通常BOM 最多就是3 個位元。
- Class: text
  Output: 接著，我們可以到<https://zh.wikipedia.org/wiki/位元組順序記號>查詢 常用的BOM 。這裡我們直接告訴各位同學，這三個位元不屬於任何已知的編碼所使
    用的BOM ，所以我們猜測這個檔案應該不帶BOM 。
- Class: text
  Output: 如果沒有BOM ，一種方法是使用`file`搭配`readLines` 兩個函數來 測試。如果讀出來的中文可以正常顯示，通常我們就猜中編碼了。如果讀出
    亂碼，或是讀取發生錯誤，那可能就猜錯了，就換個編碼猜猜看。
- Class: cmd_question
  Output: 請同學試試看BIG5編碼：`readLines(file(lvr_land.path, encoding = "BIG5"), n = 1)`
    這裡的file指令會打開一個檔案，第一個參數是檔案的路徑，第二個名稱為encoding的參數 代表檔案的編碼。如果是BIG5編碼，就輸入名稱：`"BIG5"`。UTF-8
    的話，就輸入`"UTF-8"`。 `readLines` 函數會一行一行的讀取檔案的內容。第一個參數就是用`file`函數開啟的檔案。 第二個名稱為n 的參數，則代表讀取的行數。這裡`n=1`
    代表讀取一行，所以只會回傳一個 元素的character 向量。
  CorrectAnswer: readLines(file(lvr_land.path, encoding = "BIG5"), n = 1)
  AnswerTests: omnitest(correctVal = readLines(file(lvr_land.path, encoding = "BIG5"),
    n = 1))
  Hint: readLines(file(lvr_land.path, encoding = "BIG5"), n = 1)
- Class: text
  Output: 一試就中，所以同學的R console 上應該顯示出正確的中文字串。
- Class: text
  Output: 這裡打個岔。`file`函數會建立一個代表檔案的物件，在R 之中叫做`connection`。 R 會不定時的關閉不使用的connection，並且在console
    顯示警告訊息。 所以之後同學會不定時的看到：`closing unused connection`。
- Class: cmd_question
  Output: 一開始先用`readLines`看一下資料是個好習慣。除了第一行外，我們應該 多看幾行，好猜猜看檔案的格式。請同學再輸入一次`file` 和`readLines`
    的組 合指令，並且讀取5 行。
  CorrectAnswer: readLines(file(lvr_land.path, encoding = "BIG5"), n = 5)
  AnswerTests: omnitest(correctVal = readLines(file(lvr_land.path, encoding = "BIG5"),
    n = 5))
  Hint: 上一次我們使用的指令是：`readLines(file(lvr_land.path, encoding = "BIG5"), n = 1)` 如果不知道怎麼改`readLines`
    讀取的行數，可以回想一下參數n 的意義。
- Class: text
  Output: 有時候，由於不明原因，R 還是會發生無法讀取特定編碼的狀況。 這時候，我們可以先把資料以位元的方式讀取到R 中，再做轉碼。
- Class: text
  Output: 在程式的世界中，資料在電腦中最終都是儲存為0 和1 的位元。 在處理資料時，程式需要先對資料有一些認識才能做處理，例如把資料當成 字串、整數或浮點數。文字的資料才會被編碼影響，所以如果我們只是用位元
    來處理檔案的內容，就可以避開編碼的問題。
- Class: cmd_question
  Output: R 是型態為raw 的向量來處理位元的物件。只要能知道檔案的大小， readBin 就可以把所有的檔案資料以raw 的形式輸入到R 中。我們可以使用
    `file.info`來查詢檔案的大小。請同學利用`file.info(lvr_land.path)` 來查詢檔案的資訊，並且把結果存到變數`lvr_land.info`。
  CorrectAnswer: lvr_land.info <- file.info(lvr_land.path)
  AnswerTests: any_of_exprs("lvr_land.info <- file.info(lvr_land.path)", "lvr_land.info
    = file.info(lvr_land.path)")
  Hint: lvr_land.info <- file.info(lvr_land.path)
- Class: cmd_question
  Output: 請同學先輸入`class(lvr_land.info)`
  CorrectAnswer: class(lvr_land.info)
  AnswerTests: any_of_exprs("class(lvr_land.info)")
  Hint: class(lvr_land.info)
- Class: mult_question
  Output: 請問同學，`file.info`的回傳值是什麼型態？
  AnswerChoices: data.frame;character;numeric
  CorrectAnswer: data.frame
  AnswerTests: omnitest(correctVal = "data.frame")
- Class: cmd_question
  Output: 接下來請同學列出`lvr_land.info`的欄位名稱
  CorrectAnswer: colnames(lvr_land.info)
  AnswerTests: omnitest("colnames(lvr_land.info)")
  Hint: colnames(lvr_land.info)
- Class: cmd_question
  Output: 請問同學，`lvr_land.path`背後的檔案大小，應該是多少呢？ 請輸入一個指令，讓R 輸出一個數值，代表這個檔案的大小。
  CorrectAnswer: lvr_land.info$size
  AnswerTests: omnitest(correctVal = lvr_land.info$size)
  Hint: lvr_land.info$size
- Class: cmd_question
  Output: 接著請同學利用`readBin`來把`lvr_land.path`的檔案完全以 位元組的方式讀到R 中，並且存到名稱為`lvr_land.bin`的變數。
  CorrectAnswer: lvr_land.bin <- readBin(lvr_land.path, what = "raw", n = lvr_land.info$size)
  AnswerTests: any_of_exprs('lvr_land.bin <- readBin(lvr_land.path, what = "raw",
    n = lvr_land.info$size)', 'lvr_land.bin = readBin(lvr_land.path, what = "raw",
    n = lvr_land.info$size)')
  Hint: lvr_land.bin <- readBin(lvr_land.path, what = "raw", n = lvr_land.info$size)
- Class: cmd_question
  Output: 接著，請同學安裝stringi套件。這個套件提供很多解決編碼問題的函數。 未來同學如果在R 上遇到任何和文字編碼相關的問題，我也都很建議從這個套件中
    尋找解決方法。
  CorrectAnswer: check_then_install("stringi", "1.0.1")
  AnswerTests: test_package_version("stringi", "1.0.1")
  Hint: 請利用`install.packages`或是透過Rstudio的功能安裝套件
- Class: cmd_question
  Output: 請同學載入stringi
  CorrectAnswer: library(stringi)
  AnswerTests: test_search_path("stringi")
  Hint: library(stringi)
- Class: text
  Output: 透過stringi套件的`stri_encode`，我們可以把一個位元組的向量（在 R 中，這類向量的型態是raw ）從一個編碼轉換為另一個編碼。
- Class: cmd_question
  Output: 請同學把`stri_encode(lvr_land.bin, "BIG-5", "UTF-8")` 的結果存到`lvr_land.txt`之中。
  CorrectAnswer: lvr_land.txt <- stri_encode(lvr_land.bin, "BIG-5", "UTF-8")
  AnswerTests: any_of_exprs('lvr_land.txt <- stri_encode(lvr_land.bin, "BIG-5", "UTF-8")',
    'lvr_land.txt = stri_encode(lvr_land.bin, "BIG-5", "UTF-8")')
  Hint: lvr_land.txt <- stri_encode(lvr_land.bin, "BIG-5", "UTF-8")
- Class: text
  Output: 透過以上的方法，我們可以取得轉換編碼後的character 向量`lvr_land.txt`。
- Class: text
  Output: 接著我們開始來講解`read.table`這個讀取CSV 格式檔案最泛用的指令。
- Class: mult_question
  Output: 在R 中，`read.table`的第一個參數和readLines 的第一個參數一樣，代表要開啟的檔案。 如果我們不在乎檔案的編碼，可以直接輸入檔案的路徑。以下哪一個是正確的路徑？
  AnswerChoices: lvr_land.path;"lvr_land.path"
  CorrectAnswer: lvr_land.path
  AnswerTests: omnitest(correctVal = 'lvr_land.path')
  Hint: 在R 中，`lvr_land.path`代表一個變數，但是`"lvr_land.path"`代表一個字串。
- Class: mult_question
  Output: 可惜我們需要設定編碼。（註：在中文版windows 上預設會使用BIG5編碼，所以可以 直接如上題輸入`lvr_land.path`。第二個重要的參數，是`header`。`header
    = TRUE`代表 檔案的第一行是欄位名稱，而不是資料。`header=FALSE`代表檔案的第一行就是資料。剛剛 我們看到檔案的前五行的內容是：`[1]
    "鄉鎮市區,交易標的,..." [2] "文山區,房地(土地+ 建物)..."`。請問同學，`header`的參數應該要給哪一個呢？
  AnswerChoices: TRUE;FALSE
  CorrectAnswer: 'TRUE'
  AnswerTests: omnitest(correctVal = "TRUE")
  Hint: 仔細想想，第一行是資料，還是欄位名稱呢？
- Class: mult_question
  Output: 第三個重要的參數是`sep` ，這代表各行欄位間的分隔符號。 根據之前我們看到的前五行的資料：`[1] "鄉鎮市區,交易標的,土地區段位置...,土地移轉總面積..."
    [2] "文山區,房地(土地+建物),臺北市文山區...,43.44..." [3] "中正區,房地(土地+建物),臺北市中 正區...,40.19..."。請問同學，這份檔案個欄位的分隔符號是什麼呢？
  AnswerChoices: -;\t;,
  CorrectAnswer: ','
  AnswerTests: omnitest(correctVal = ",")
  Hint: 從第2 行，我們可以判斷，`"文山區"`應該代表`"鄉鎮市區"`，而`"房地(土地+建物)"` 應該代表`"交易標的"`，以此類推。`"鄉鎮市區"`和`"房地(土地+建物)"`
    兩筆資料中間差一 個`","`。而如果我們把`","`當分隔符號去解釋剩下的資料，也解釋得通。
- Class: cmd_question
  Output: 依照以上問答題的答案，請同學利用`read.table`，用BIG5編碼讀取 `lvr_land.path` ，並且把輸出存到變數`lvr_land`
  CorrectAnswer: lvr_land <- read.table(file(lvr_land.path, encoding = "BIG5"), header
    = TRUE, sep = ",")
  AnswerTests: val_is(read.table(file(lvr_land.path, encoding = "BIG5"), header =
    TRUE, sep = ","), "lvr_land")
  Hint: lvr_land <- read.table(file(lvr_land.path, encoding = "BIG5"), header = TRUE,
    sep = ",")
- Class: cmd_question
  Output: 如果我們要直接從`lvr_land.txt`的內容取出表格，一種方法是直接把 `lvr_land.txt`寫成一個檔案，再用`read.table`讀取。另外一種方法，是把
    `lvr_land.txt`視為一個類似檔案的物件，也就是connection。我們可以用 `textConnection(lvr_land.txt)`來從`lvr_land.txt`建立一個connection。
    請同學輸入：`read.table(textConnection(lvr_land.txt), header = TRUE, sep = ",")`
  CorrectAnswer: read.table(textConnection(lvr_land.txt), header = TRUE, sep = ",")
  AnswerTests: omnitest('read.table(textConnection(lvr_land.txt), header = TRUE, sep
    = ",")')
  Hint: read.table(textConnection(lvr_land.txt), header = TRUE, sep = ",")
- Class: text
  Output: 以上提供數種在R 中處理中文資料的方法給同學參考。
- Class: text
  Output: 事實上，如果同學自行下載原本的檔案來處理，上述的流程還是會出錯的。 那是因為在第21行，資料的最後一欄的內容包含：`,`這個符號，導致欄位判斷出錯。
- Class: text
  Output: 遇到這種狀況，最好的方式是自己用如notepad++或是其他的編輯器，自行編輯 原始的檔案資料（依照錯誤訊息，移除多餘的,），再用R 讀取。由於這樣的動作超過
    這個課程系統的內容，所以我們就不帶著同學做操作了。
- Class: text
  Output: 另外一種方式，是透過`readLines` 來讀取資料，再用R 的其他函數自行把讀入 的字串轉換為data.frame。這部分很難歸納，會隨著資料的乾淨程度而有所不同。同學
    可以再閱讀本課程中的，一些實際分析Open Data 的範例，就可以學到一些技巧。或是 繼續學習R 的字串處理函數與套件。
- Class: text
  Output: 我們在這門課程中簡單展示了如何從套件中載入資料，以及如何從檔案中讀取 CSV 的資料。
- Class: text
  Output: 如果要使用R 讀取XML 的資料，可以使用套件XML 。如果要讀取JSON， 可以使用rjsonlite 。只要資料格式是公開格式，我們很容易找到R
    的套件來 讀取該資料格式。這就是R 是Open Source 的威力！
- Class: text
  Output: 受限於課程內容，所以我們這裡只講讀取CSV 的方式。這應該是最泛用 的內容了。未來有時間，會再找社群的其他大大合作開寫其他和讀資料相關的 課程。同學如果很期待看到處理某些形態資料的課程，歡迎到
    <https://github.com/wush978/DataScienceAndR/issues> 許願。
- Class: script
  Output: 最後，還是要請同學利用這次所學的內容，做一個小練習。 請同學在完成之後存檔，並輸入`submit()`來檢查結果是否符合預期。 如果同學在檔案中看到亂碼，請使用Rstudio
    左上角的File -> Reopen With Encoding... -> 選取：UTF-8
  Script: RBasic-07-HW.R
  AnswerTests: rbasic_07_hw_test()

