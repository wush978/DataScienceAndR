# 請同學計算x1的樣本變異數
x1.var <- local({
  # 請填寫你的程式碼
  var(x1)
})

# 請同學計算x1的樣本標準差
x1.sd <- local({
  # 請填寫你的程式碼
  sd(x1)
})

# 請同學計算x2的樣本變異數
x2.var <- local({
  # 請填寫你的程式碼
  var(x2)
})

# 請同學計算x2的樣本標準差
x2.sd <- local({
  # 請填寫你的程式碼
  sd(x2)
})

# 請同學計算x3的樣本變異數
x3.var <- local({
  # 請填寫你的程式碼
  var(x3)
})

# 請同學計算x3的樣本標準差
x3.sd <- local({
  # 請填寫你的程式碼
  sd(x3)
})

x4.var <- var(x4)
x4.sd <- sd(x4)
# 請同學回到console輸入`submit()`檢查答案
