# 請同學計算x1的最大值
x1.max <- local({
  max(x1)
})


# 請同學計算x1的最小值
x1.min <- local({
  min(x1)
})

# 請同學計算x1的全距
x1.range <- local({
  diff(range(x1))
})

# 請同學計算x2的最大值
x2.max <- local({
  max(x2)
})


# 請同學計算x2的最小值
x2.min <- local({
  min(x2)
})

# 請同學計算x2的全距
x2.range <- local({
  diff(range(x2))
})

# 請同學計算x3的最大值
x3.max <- local({
  max(x3)
})


# 請同學計算x3的最小值
x3.min <- local({
  min(x3)
})

# 請同學計算x3的全距
x3.range <- local({
  diff(range(x3))
})

x4.min <- min(x4)
x4.max <- max(x4)
x4.range <- diff(range(x4))
# 請同學回到console輸入`submit()`檢查答案

