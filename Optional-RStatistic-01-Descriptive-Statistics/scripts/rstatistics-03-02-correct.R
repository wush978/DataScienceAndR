# 請同學計算x1的全距
x1.MD <- local({
  # 請填寫你的程式碼
  mean(abs(x1 - mean(x1)))
})

# 請同學計算x2的全距
x2.MD <- local({
  # 請填寫你的程式碼
  mean(abs(x2 - mean(x2)))
})

# 請同學計算x3的全距
x3.MD <- local({
  # 請填寫你的程式碼
  mean(abs(x3 - mean(x3)))
})

x4.MD <- mean(abs(x4 - mean(x4)))

# 請同學回到console輸入`submit()`檢查答案
