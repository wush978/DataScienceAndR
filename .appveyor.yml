# DO NOT CHANGE the "init" and "install" sections below

# Download script file from GitHub
init:
  ps: |
        $ErrorActionPreference = "Stop"
        Invoke-WebRequest http://raw.github.com/wush978/r-appveyor/master/scripts/appveyor-tool.ps1 -OutFile "..\appveyor-tool.ps1"
        Import-Module '..\appveyor-tool.ps1'
install:
  ps: Bootstrap

# Adapt as necessary starting from here
environment:
  matrix:
  - R_VERSION: release
    R_ARCH: x64
    CRAN: http://cran.rstudio.com

build_script:

build_script:
  - Rscript -e "source('http://wush978.github.io/DataScienceAndR/init.R')"

test_script:
  - Rscript .test_course.R push

on_failure:
  - travis-tool.sh dump_logs
