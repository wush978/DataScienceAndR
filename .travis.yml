language: r

sudo: required

r_binary_packages:
  - swirl

script:
  - Rscript -e "source('https://raw.githubusercontent.com/wush978/DataScienceAndR/master/test_lesson.R')"

install:
  - Rscript -e "setwd('..');install_course_directory('DataScienceAndR')"

notifications:
  email:
    on_success: change
    on_failure: change
env:
  - global:
    - WARNINGS_ARE_ERRORS=1
    - BOOTSTRAP_LATEX=0
